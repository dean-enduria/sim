'use client'

/**
 * Stubbed AccessControl settings component.
 * RBAC is managed by Enduria — this panel is no longer needed.
 */
export function AccessControl() {
  return (
    <div className="p-4 text-sm text-muted-foreground">
      Access control is managed externally.
    </div>
  )
}
