'use client'

/**
 * Stubbed SSOAuth component.
 * SSO is managed by Enduria — this flow is no longer needed.
 */

interface SSOAuthProps {
  identifier: string
}

export default function SSOAuth(_props: SSOAuthProps) {
  return null
}
