'use client'

/**
 * Stubbed SSO settings component.
 * SSO is managed by Enduria via SSOReady — this panel is no longer needed.
 */
export function SSO() {
  return (
    <div className="p-4 text-sm text-muted-foreground">
      SSO is managed externally.
    </div>
  )
}
